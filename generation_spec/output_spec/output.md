In addition to the funscript finally selected, the user may want to do additional hand editing in funscriptforge. That's where the estim and haptic are generated. 

To inform the decision of the user, this application should store a json document that captures the suggestions made by the generator, even if they were not selected. 

Those data in the json document are envisioned to become user-built-project transformer plugins. And available to the user during hand editing. 

but the idea is that the user ccould revist some of the decisions made, and make informed decisions for a particular section, with not only the standard transformer patters in funscriptforge, but could use the transforms. 

PROBABLY A BETTER IDEA

the other way to do this, is to make the generator code available as a transform. User could select the sliders, and rebuild that section to their liking.