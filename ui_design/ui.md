# UI design plan

System: FunscriptForge red accent on dark editor backdrop. Pathway shell (4 numbered stages). Polished reinterpretation of the scaffold — not a 1:1 port.

Big buck bunny: I couldn't find a real BBB sample in forgegen/test_media. I'll use the real funscript shape but with synthetic big_buck_bunny-style data (5–7 chapters: intro / build / encounter / confrontation / drop / outro) so the data is plausible and stable. If you have a specific BBB file you want me to ingest, drop it in and I'll wire it up.

Generate dual-mode: I'll wire Tweaks → "Authoring mode: Table | Visual" so the user can flip between the dense table UI you have today and the per-chapter visual editor (with zoom that doesn't suck this time).

Novel UX: per-chapter heat-ribbon, beat-locked playhead pulse, animated "forge" sequence during Generate, and an automatic chapter-zoom transition on the funscript chart.